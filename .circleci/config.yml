version: 2
jobs:
  build:
    working_directory: ~/circulate
    docker:
      - image: python:3.6.0
      - image: ruby:2.4.1
    steps:
      - checkout
      - run: echo "hello world"
      - run: echo `ruby --version`
      - run: python --version
